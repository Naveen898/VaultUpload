{
  "name": "vaultupload-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm --prefix frontend install && npm --prefix frontend run dev",
    "frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  }
}
